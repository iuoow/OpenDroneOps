# ADR 0009: Design Token First

- 状态：Accepted
- 日期：2026-07-26

## 背景

生成式开发容易在不同页面产生不一致的颜色、间距、圆角、阴影和 z-index。

## 决策

颜色、排版、间距、圆角、控件尺寸、动效和布局基线必须来自 `docs/design/tokens/`。新 Token 需要评审。

## 结果

页面一致且易支持主题；限制局部随意设计。组件必须使用语义 Token，而不是直接使用状态 Hex 值。

## 备选

使用组件库默认主题或页面内硬编码；不采纳。
